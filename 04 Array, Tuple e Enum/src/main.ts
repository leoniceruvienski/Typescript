// Array, Tuple, Enum
// Array

let items: Array<number>
items = [1, 2, 3, 4, 'estudar']


let items: number[]
items = [1, 2, 3, 4]

// // Tuple
let items: [number, string, boolean]
items = [1, 'cataline', true]

// // enum
enum colors {
    white = '#fff',
    black= '#000'
}
let white: colors = colors.black
console.log(white)